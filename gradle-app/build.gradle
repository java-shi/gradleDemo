//增加插件
apply plugin:'org.springframework.boot'

apply plugin: 'war'

//依赖管理
dependencies {
    //模块依赖
    api project(":gradle-common")
    api project(":gradle-entity")

    compile("org.springframework.boot:spring-boot-starter-web") {
        exclude module: "spring-boot-starter-tomcat"
    }
}

